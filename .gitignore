# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# OS
.DS_Store

# Local state (never commit)
*.state
*.tmp

# Source PDFs (license-aware: never commit owned standard PDFs)
sources/**/*.pdf
!sources/**/*.md

# internal acquisition tracking lives in the KB, never here
sources/

# prepack copies this in from the repo root so the tarball is
# self-contained; the checked-in copy would be a second source of truth
packages/aero-agent-roles/scripts/evidence.py
